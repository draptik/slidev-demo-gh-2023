name: Deploy pages
on: push
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
      - name: Install dependencies
        run: |
            cd presentation-demo
            npm install
      - name: Install slidev
        run:  |
            cd presentation-demo
            npm i -g @slidev/cli
      - name: Build
        run: |
            cd presentation-demo
            slidev build --base /slidev-demo-gh-2023
      
      # - name: Deploy pages
      #   uses: crazy-max/ghaction-github-pages@v2
      #   with:
      #     build_dir: presentation-demo/dist
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: presentation-demo/dist # The folder the action should deploy.
          target-folder: demo1
          single-commit: true

  # deploy-custom:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v2
  #     - uses: actions/setup-node@v2
  #       with:
  #         node-version: '14'
  #     - name: Install dependencies
  #       run: |
  #           cd presentation-custom
  #           npm install
  #     - name: Install slidev
  #       run:  |
  #           cd presentation-custom
  #           npm i -g @slidev/cli
  #     - name: Build
  #       run: |
  #           cd presentation-custom
  #           slidev build --base /slidev-demo-gh-2023/bar
  #     - name: Deploy pages
  #       uses: crazy-max/ghaction-github-pages@v2
  #       with:
  #         build_dir: presentation-custom/dist
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
               
